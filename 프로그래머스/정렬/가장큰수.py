def solution(numbers):
    answer = ''
    visit = list()
    for i in range(len(numbers)):
        max_value = str(numbers[i])[0]
        visited.append(max_value)
        while len(visit)==len(numbers):
            abs


    return answer
solution([3, 30, 34, 5, 9])